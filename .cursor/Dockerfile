# Utilise une image officielle Node.js
FROM node:18

# Définit le dossier de travail
WORKDIR /workspace

# Installe les dépendances globales si besoin (optionnel)
RUN npm install -g npm

# (Ne pas copier le code du projet ici, Cursor le clone depuis GitHub)